# Umnico Auto Greeting Bot

Автоматически отправляет приветствие (голосовое, видео, фото или документ) всем новым клиентам в непринятых диалогах (inbox) указанной интеграции.

## Особенности

- ✅ Отправляет приветствие только **новым клиентам** (по `customer.id`)
- ✅ Работает с **непринятыми диалогами** (inbox) - обращения, которые еще не взяты в работу
- ✅ Проверяет, является ли это **первое обращение клиента** в интеграции
- ✅ Поддержка разных типов файлов: audio, video, photo, doc
- ✅ Настраиваемая интеграция через `.env`
- ✅ Обработка всех сетевых ошибок
- ✅ Логирование в файл с UTF-8 кодировкой
- ✅ Graceful shutdown (Ctrl+C)

## Установка

```bash
pip install requests python-dotenv
```

## Настройка

Создайте файл `.env` рядом с `app.py` (используйте `.env.example` как шаблон):

```env
# Umnico API Token (Umnico → Настройки → API Public)
UMNICO_TOKEN=your_api_token_here

# Файл для отправки (путь относительно app.py)
GREETING_FILE=Салем_1.ogg

# Тип файла: audio, video, photo, doc
FILE_TYPE=audio

# ID интеграции (найти в Umnico → Интеграции)
TARGET_SA_ID=108954

# Интервал опроса API в секундах (по умолчанию: 10)
POLL_INTERVAL=10

# Файл для логов (по умолчанию: bot.log)
LOG_FILE=bot.log
```

### Как найти ID интеграции

1. **ID интеграции (TARGET_SA_ID)**:
   - Откройте Umnico → Интеграции
   - Найдите нужную интеграцию
   - ID можно получить через API: `GET /v1.3/integrations`

## Запуск

```bash
python app.py
```

## Типы файлов

Бот поддерживает следующие типы файлов:

- `audio` - аудиофайлы (.ogg, .mp3, .wav)
- `video` - видеофайлы (.mp4, .avi, .mov)
- `photo` - изображения (.jpg, .png, .gif)
- `doc` - документы (.pdf, .docx, .txt)

## Логика работы

1. При запуске бот загружает всех существующих клиентов из непринятых диалогов (inbox)
2. Каждые N секунд (POLL_INTERVAL) опрашивает API на наличие новых непринятых обращений
3. Для каждого нового клиента проверяет через API, является ли это его первое обращение в интеграции
4. Если это первый контакт - отправляет приветствие
5. Каждый клиент получает приветствие **только один раз**, даже если создаст несколько обращений

## Файлы

| Файл | Описание |
|---|---|
| `app.py` | Основной скрипт |
| `Салем_1.ogg` | Пример голосового приветствия |
| `.env` | Конфигурация (создать самостоятельно) |
| `.env.example` | Шаблон конфигурации |
| `bot.log` | Файл логов (создается автоматически) |

## Требования

- Python 3.7+
- requests
- python-dotenv
